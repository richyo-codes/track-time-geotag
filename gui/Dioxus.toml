[application]
name = "track-time-tagger"

[web.app]
title = "Track Time Tagger"

[web.resource]
style = ["assets/main.css"]

# Required by the Dioxus 0.7 configuration schema. Development uses the
# regular stylesheet above and adds no development-only resources.
[web.resource.dev]
